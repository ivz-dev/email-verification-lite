# --- HTTP server ---
# Port the API listens on.
PORT=3000
# Interface to bind. 0.0.0.0 = all interfaces; 127.0.0.1 = localhost only.
HOST=0.0.0.0

# --- Verification ---
# Timeout for DNS (MX/A/AAAA) lookups, in milliseconds.
DNS_TIMEOUT_MS=5000
# Maximum number of addresses accepted by POST /verify/batch.
MAX_BATCH_SIZE=100
